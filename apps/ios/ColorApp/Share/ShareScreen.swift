//
//  ShareScreen.swift
//  ColorApp
//

import SwiftUI

/// 空殼。實際的匯出要等 `exportPNG` / `exportTimelapse` 真的實作（E1 / E3）。
struct ShareScreen: View {
    var body: some View {
        Text("Share")
            .navigationTitle("Share")
            .navigationBarTitleDisplayMode(.inline)
    }
}
