/target

# baker 產物：走 R2，不進 git（architecture.md §12.2）
/assets/packs/

# uniffi 產物：CI 每次重建，並由 `cargo xtask verify-generated` 檢查新鮮度
/apps/ios/Generated/
/apps/android/generated/

# Xcode 的個人狀態，不是專案設定（specs/ios-scaffold.md §2）。
# `UserInterfaceState.xcuserstate` 每次開關 Xcode 都會變，scheme 管理則是個人偏好。
# 要進 git 的 scheme 一律放 `xcshareddata/xcschemes/`——CI 的 `-scheme` 只讀得到那裡。
xcuserdata/
DerivedData/

.DS_Store
